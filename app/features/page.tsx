import CategoryPage from "@/components/CategoryPage";

export const metadata = { title: "Features — The Beacon" };

export default function FeaturesPage() {
  return <CategoryPage category="features" />;
}
